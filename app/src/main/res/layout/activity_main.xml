<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="25sp"
        android:textColor="@color/black"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnIncreaseCounter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:text="Increase"
        app:layout_constraintEnd_toEndOf="@+id/tv_counter"
        app:layout_constraintStart_toStartOf="@+id/tv_counter"
        app:layout_constraintTop_toBottomOf="@+id/tv_counter" />

    <Button
        android:id="@+id/btnLongTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Long Running Task"
        app:layout_constraintEnd_toEndOf="@+id/btnIncreaseCounter"
        app:layout_constraintStart_toStartOf="@+id/btnIncreaseCounter"
        app:layout_constraintTop_toBottomOf="@+id/btnIncreaseCounter" />

</androidx.constraintlayout.widget.ConstraintLayout>